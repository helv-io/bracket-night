/**
 * Live-room rules live in the engine package now.
 * This file stays so older npm test -w backend scripts do not 404.
 */
console.log('backend/game.test.ts: engine owns matchup tests (npm test -w engine)')
